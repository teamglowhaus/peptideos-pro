# Server-side only — never exposed to the browser
ANTHROPIC_API_KEY=sk-ant-...
PEXELS_API_KEY=your-pexels-key

# Optional: sell access to a hosted instance.
# Comma-separated codes; visitors must enter one to unlock the app.
# Leave unset to keep the app open (personal use).
# ACCESS_CODES=sunrise-42,vip-panda,launch-code
